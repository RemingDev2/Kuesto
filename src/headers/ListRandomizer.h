#ifndef KUESTO_LISTRANDOMIZER_H
    #define KUESTO_LISTRANDOMIZER_H

#include <vector>
#include <string>

class ListRandomizer {

public:
    static void randomize(std::vector<std::string> contentToRandomize);

};

#endif
